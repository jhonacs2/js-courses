---
title: "Fundamentos de CSS Grid"
description: "Aprende los fundamentos de CSS Grid: tracks, lines, cells, fr, repeat(), minmax() y columnas dinámicas."
order: 1
section: "css-grid"
type: "theory"
duration: "1h 30min"
topics:
  - "display: grid y terminología"
  - "grid-template-columns y grid-template-rows"
  - "Unidad fr, repeat(), minmax()"
  - "gap, row-gap, column-gap"
  - "auto-fill vs auto-fit"
---

## Temas que cubrirás

- Flexbox vs Grid: cuándo usar cada uno (1D vs 2D)
- `display: grid` — terminología: tracks, lines, cells, areas
- `grid-template-columns` y `grid-template-rows`
- Unidad `fr` — fracciones del espacio disponible
- `repeat()` y `minmax()` — tamaños flexibles
- `gap`, `row-gap`, `column-gap`
- `auto-fill` vs `auto-fit`

## Reto

Crear una galería de imágenes que se adapte automáticamente a cualquier tamaño de pantalla usando `auto-fit` y `minmax`.
